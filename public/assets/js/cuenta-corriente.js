// ============================================
// MÓDULO CUENTA CORRIENTE
// ============================================

const CuentaCorrienteModule = (function() {

    function init() {
    }

    return {
        init: init
    };
})();

document.addEventListener('DOMContentLoaded', CuentaCorrienteModule.init);